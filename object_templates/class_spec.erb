# frozen_string_literal: true

require 'spec_helper'

describe '<%= name %>' do
  on_supported_os.each do |os, os_facts|
    context "on #{os}" do
      let(:facts) { os_facts }
      let(:node) { 'foo.bar' }

      it { is_expected.to compile }
    end
  end
end
